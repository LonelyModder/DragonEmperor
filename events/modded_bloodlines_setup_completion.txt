namespace = bloodlines_setup_completion

bloodlines_setup_completion.1 = {
	type = country_event
	hidden = yes
	
	immediate = {
		every_character = {
			limit = {
				is_alive = yes
				OR = {
					exists = father
					exists = mother
				}
			}
			if = {
				limit = {
					exists = father
					father = {
						has_trait = aranas
					}
					NOT = {
						has_trait = aranas
					}
				}
				add_trait = aranas
			}
			
			if = {
				limit = {
					exists = mother
					mother = {
						has_trait = aranas
					}
					NOT = {
						has_trait = aranas
					}
				}
				add_trait = aranas	
			}
			
		}
	}
}