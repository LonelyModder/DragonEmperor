
character_events.35 = {
	type = character_event
	hidden = yes
	
	immediate = {
		if = {
			limit = {
				is_alive = yes
				exists = father
				father = {
					OR = {
						has_trait = davidids
					}
				}
			}
			if = {
				limit = {
					father = {
						has_trait = davidids
					}
					NOT = {
						has_trait = davidids
					}
				}
				add_trait = davidids
			}
			
		}
		if = {
			limit = {
				exists = mother
				mother = {
					has_trait = davidids
				}
				NOT = {
					has_trait = davidids
				}
			}
			add_trait = davidids	
		}
		
	}
}